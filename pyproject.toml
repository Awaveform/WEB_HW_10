[tool.poetry]
name = "web-hw-10"
version = "0.1.0"
description = ""
authors = ["Antonii"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
django = "^5.0.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
